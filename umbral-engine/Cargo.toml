[package]
name = "umbral-engine-zos"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
aya = "0.12"
aya-log = "0.2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
